{
  "version": "1.0.6",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "build": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "echo no tests to run",
    "prepare": "husky install"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@dwmkerr/standard-version": "^8.3.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.8.0",
    "husky": "^7.0.4"
  }
}
